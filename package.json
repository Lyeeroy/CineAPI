{
  "name": "cineapi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css",
    "watch": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.1",
    "tailwindcss": "^4.0.5"
  },
  "_comment": "Remember to run 'npm run build' to compile Tailwind CSS before launching the project. 'npm run watch' to watch for changes."
}
