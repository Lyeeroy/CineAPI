module.exports = {
  darkMode: false, // Ensures dark mode is applied via class, not system preference
  content: ["./**/*.html", "./src/**/*.js"],
  theme: {
    extend: {},
  },
  plugins: [],
};
